using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using VirtualCar.Models;

namespace VirtualCar.Controllers.FrontEndVirtualCar
{
    public class Utils
    {

        //Persona[] personas = { new Persona(10, "isragoo"), new Persona(25, "israel"), new Persona(15, "prez") };
        //var respersona = (from p in personas select new { nom = p.nombre, ed = p.edad });


        //int[] numbers = { 5, 4, 1, 3, 9, 8, 6, 7, 2, 0 };
        //var resultado = from n in numbers select new { num = n, npar = (n % 2) == 0 };
        //var resmaydos = from n in numbers where n < 2 select new { num = n, npar = (n % 2) == 0 };
        //var respersonasLambda = (personas.Where(p => { return p.edad == 15; }));
        //var respersonasLambda2 = (personas.Where(p => { return p.edad == 15; })).Select(q => { return new { q.nombre }; });
        //@{ var company = (CompanyInfo)ViewData.Model; }

    }
}